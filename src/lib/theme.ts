/**
 * St. Cecilia's Girls' College brand theme colors.
 * Used for Leaflet canvas/SVG markers and components needing raw JS values.
 * Must match the CSS variables in src/style.css.
 */
export const THEME = {
  primary: '#1E3A8A',       // --brand-primary
  primaryDark: '#152C6B',   // --brand-primary-dark
  primaryLight: '#EEF2FF',  // --brand-primary-light
} as const;
